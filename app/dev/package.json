{
  "name": "foxx-pet-dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "astro sync",
    "fetch": "DOTENV_CONFIG_PATH=.env node ../../scripts/fetch-immich.mjs --target=dev",
    "fetch-quiet": "DOTENV_CONFIG_PATH=.env DOTENV_SILENT=true node --redirect-warnings=../../dev-fetch.log ../../scripts/fetch-immich.mjs --target=dev --quiet",
    "gen": "npm run fetch && npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "astro": "^5.13.5"
  }
}
