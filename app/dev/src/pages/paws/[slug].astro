---
import { View } from '@Frontend';
import { buildCategoryStaticPaths } from '@utils/sectionPaths';

export const prerender = true;

export async function getStaticPaths() {
  return buildCategoryStaticPaths('paws');
}

const { slug } = Astro.params;

if (!slug) {
  throw new Error('Missing album category slug');
}
---
<View.Category section="paws" slug={slug} />
