{
  "name": "foxx-pet-dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "astro sync",
    "fetch": "DOTENV_CONFIG_PATH=.env node ../scripts/fetch-immich.mjs",
    "fetch-quiet": "DOTENV_CONFIG_PATH=.env DOTENV_SILENT=true node --redirect-warnings=../dev-fetch.log ../scripts/fetch-immich.mjs --quiet",
    "gen": "npm run fetch && npm run build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "astro": "^5.13.5",
    "jsdom": "^27.2.0",
    "vitest": "^2.1.4"
  }
}
