// src/scripts/galleryViewer.js
// Backwards-compatible entry point to the media handler utilities.
export { initMediaFrame, initMediaThumbs, initMediaLightbox, initMediaHandler as default } from './mediaHandler.js';
