{
  "name": "foxx.pet",
  "private": true,
  "type": "module",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "fetch": "npm run -w app fetch",
    "fetch-quiet": "npm run --silent --loglevel=silent -w app fetch-quiet",
    "build": "npm run -w app build",
    "preview": "npm run -w app preview",
    "sync": "npm run -w app sync",
    "dev": "npm run -w app dev"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "sharp": "^0.33.4",
    "undici": "^7.16.0"
  }
}
